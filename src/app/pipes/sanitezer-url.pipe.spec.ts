import { SanitezerUrlPipe } from './sanitezer-url.pipe';

describe('SanitezerUrlPipe', () => {
  it('create an instance', () => {
    const pipe = new SanitezerUrlPipe();
    expect(pipe).toBeTruthy();
  });
});
